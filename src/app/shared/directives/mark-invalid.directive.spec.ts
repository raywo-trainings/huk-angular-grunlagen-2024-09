import {MarkInvalidDirective} from './mark-invalid.directive';


describe('MarkInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
